<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.provider.mapper.StudentMapper">

    <insert id="insert" parameterType="com.example.common.entity.Student">
        INSERT INTO student (name, gender, create_time, update_time, is_deleted)
        VALUES (#{student.name}, #{student.gender}, #{student.createTime}, #{student.updateTime}, 0)
    </insert>

    <update id="update" parameterType="com.example.common.entity.Student">
        UPDATE student
        SET name = #{student.name},
            gender = #{student.gender},
            update_time = #{student.updateTime}
        WHERE id = #{student.id}
    </update>

</mapper>